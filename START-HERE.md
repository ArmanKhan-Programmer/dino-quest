# Start Dino Quest

Open `DinoQuest.html` in Chrome, Edge, or any modern browser. It is the browser-safe, standalone game entry point and works when opened directly from your computer.

`index.html` remains the modular Replit entry point; browsers deliberately block ES-module imports when it is opened directly as a `file://` URL. Replit serves it over HTTP, where those modules load normally.
