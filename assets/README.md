# Dino Quest assets

The game uses procedural canvas art and synthesized sounds so it can run instantly on Replit without downloading binary assets. Add optional images or music here later; keep their paths relative to `index.html`.
